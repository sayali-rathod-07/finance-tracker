# Personal Finance Tracker

A simple CLI application to track expenses, validate transactions, and generate financial reports.

## Setup
1. Install dependencies: `pip install -r requirements.txt`
2. Run the app: `python main.py`
3. Run tests: `pytest`
